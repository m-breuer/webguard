worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
        '$status $body_bytes_sent "$http_referer" "$http_user_agent" '
        'request_id=$request_id upstream=$upstream_addr upstream_status=$upstream_status';

    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    upstream laravel {
        server php:8080;
    }

    upstream sveltekit {
        server frontend:3000;
    }

    server {
        listen 8080;
        server_name _;
        client_max_body_size 100m;

        location = /_health/gateway {
            access_log off;
            add_header Cache-Control 'no-store' always;
            return 204;
        }

        location = /_health/frontend {
            proxy_pass http://sveltekit/_health/frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location = /_health/laravel {
            proxy_pass http://laravel/status;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location = /status {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location ~ ^/status/[^/]+/subscribers(?:/|$) {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location ~ ^/status/[^/]+/?$ {
            error_page 418 = @laravel;

            if ($request_method !~ "^GET$|^HEAD$") {
                return 418;
            }

            add_header Cache-Control 'public, max-age=30, stale-while-revalidate=60' always;
            proxy_pass http://sveltekit;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
        }

        location = /badge.js {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location = /api {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location ^~ /api/ {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location ^~ /sanctum/ {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }

        location / {
            error_page 418 = @laravel;
            error_page 404 = @laravel;

            if ($request_method !~ "^GET$|^HEAD$") {
                return 418;
            }

            proxy_intercept_errors on;
            proxy_pass http://sveltekit;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
        }

        location @laravel {
            proxy_pass http://laravel;
            include /etc/nginx/proxy-headers.conf;
        }
    }
}
