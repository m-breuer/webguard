<script lang="ts">import AdminCrudTable from "$lib/components/AdminCrudTable.svelte"; interface Props { data: { packages: { data: { items: Array<Record<string, unknown>> } } }; } let { data }: Props = $props();</script>
<svelte:head><title>Packages | WebGuard</title></svelte:head><main class="mx-auto w-[min(78rem,calc(100%_-_2rem))] py-6 sm:py-12"><AdminCrudTable title="Packages" description="Configure monitoring capacity and availability for subscription packages." singular="package" endpoint="/api/v1/internal/ui/admin/packages" items={data.packages.data.items} itemLabel={(item) => `${item.monitoring_limit} monitorings`} columns={[{ key: "monitoring_limit", label: "Monitoring limit" }, { key: "price", label: "Price", format: (value) => `€${Number(value).toFixed(2)}` }, { key: "is_selectable", label: "Selectable", format: (value) => value ? "Yes" : "No" }]} fields={[{ name: "monitoring_limit", label: "Monitoring limit", type: "number", required: true }, { name: "price", label: "Price", type: "number", required: true }, { name: "is_selectable", label: "Selectable", type: "checkbox" }]} createDefaults={{ is_selectable: true }} /></main>
