<script lang="ts">
    interface Props { page: number; pages: number; href: (page: number) => string; }
    let { page, pages, href }: Props = $props();
    const adjacent = $derived(Array.from({ length: Math.max(0, Math.min(pages, 5)) }, (_, index) => Math.min(Math.max(1, page - 2) + index, pages)));
</script>
{#if pages > 1}<nav class="flex flex-wrap gap-2" aria-label="Pagination">{#if page === 1}<span class="inline-flex min-h-10 min-w-10 items-center justify-center rounded-lg border border-wg-border px-3 py-[0.55rem] text-center text-sm font-semibold text-wg-text opacity-45" aria-disabled="true">Previous</span>{:else}<a class="inline-flex min-h-10 min-w-10 items-center justify-center rounded-lg border border-wg-border px-3 py-[0.55rem] text-center text-sm font-semibold text-wg-text no-underline" href={href(page - 1)}>Previous</a>{/if}{#each adjacent as current}<a class={`inline-flex min-h-10 min-w-10 items-center justify-center rounded-lg border px-3 py-[0.55rem] text-center text-sm font-semibold no-underline ${current === page ? "border-wg-accent bg-wg-accent text-wg-accent-contrast" : "border-wg-border text-wg-text"}`} aria-current={current === page ? "page" : undefined} href={href(current)}>{current}</a>{/each}{#if page === pages}<span class="inline-flex min-h-10 min-w-10 items-center justify-center rounded-lg border border-wg-border px-3 py-[0.55rem] text-center text-sm font-semibold text-wg-text opacity-45" aria-disabled="true">Next</span>{:else}<a class="inline-flex min-h-10 min-w-10 items-center justify-center rounded-lg border border-wg-border px-3 py-[0.55rem] text-center text-sm font-semibold text-wg-text no-underline" href={href(page + 1)}>Next</a>{/if}</nav>{/if}
